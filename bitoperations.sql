CREATE TABLE
    BITOPERATIONS
(
    ID INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY,
    I INTEGER NOT NULL,
    J INTEGER NOT NULL,
    MAXPOS INTEGER NOT NULL,
    MAXNEG INTEGER NOT NULL,
    L BIGINT NOT NULL,
    M BIGINT NOT NULL,
    LL BIGINT NOT NULL,
    LLN BIGINT NOT NULL,
    PRIMARY KEY (ID)
);